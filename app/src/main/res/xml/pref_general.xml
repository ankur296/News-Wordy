<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_sort_by_key"
        android:title="@string/pref_title_sort_by"
        android:defaultValue="-1"
        android:entries="@array/pref_sort_by_list_titles"
        android:entryValues="@array/pref_sort_by_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="@string/pref_category_key"
        android:title="@string/pref_title_category"
        android:defaultValue="-1"
        android:entries="@array/pref_category_list_titles"
        android:entryValues="@array/pref_category_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="@string/pref_adult_key"
        android:title="@string/pref_title_adult_filter"
        android:defaultValue="-1"
        android:entries="@array/pref_adult_filter_list_titles"
        android:entryValues="@array/pref_adult_filter_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
